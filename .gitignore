 /node_modules
  /build
  /coverage
  .env.local
  .env.*.local
  .DS_Store
      69 +├── static/             # CSS + JS                
      70 +├── requirements.txt                              
      71 +├── Procfile            # Railway/Gunicorn        
         +entry point                                       
      72 +└── railway.json        # Railway deploymen       
         +t config                                          
      73  ```
      74
      53 ----                                               
      75 +## 🔌 Key endpoints                               
      76
      55 -## Extending this project                         
      56 -Ideas to make it more impressive for fellow       
         -ship applications:                                
      57 -- Add a "Save brief as PDF" button                
      58 -- Let users upload their existing materials       
         - for Claude to reference                          
      59 -- Add multiple tone presets with example ou       
         -tputs                                             
      60 -- Build a history page showing past briefs        
      61 -- Deploy to Render or Railway (free hosting       
         -) so you have a live URL to share                 
      77 +| Route | Method | Purpose |                      
      78 +|-------|--------|---------|                      
      79 +| `/` | GET | Main app UI |                       
      80 +| `/generate` | POST | Generate a campaign        
         +brief (streamed) |                                
      81 +| `/personas` | POST | Generate audience pe       
         +rsonas |                                          
      82 +| `/suggestions` | POST | Content suggestio       
         +ns |                                              
      83 +| `/visuals` | POST | Visual direction sugg       
         +estions |                                         
      84 +| `/tweak` | POST | Refine an existing brie       
         +f |                                               
      85 +| `/save` · `/history` | POST · GET | Save        
         +and list briefs |                                 
      86 +| `/orgs` | GET/POST/PUT/DELETE | Manage or       
         +ganisation profiles |                             
      87 +| `/export/pdf` | POST | Export a brief to        
         +PDF |                                             
      88 +                                                  
      89 +## 🔒 Notes                                       
      90 +- No secrets are committed — the API key is       
         + supplied via environment variable only.          
      91 +- The local database (`briefs.db`), logs, a       
         +nd caches are excluded via `.gitignore`.          
